package flixel.ecs.components;

/**
 * Stores data for actions that allow the entity to respond to user input.
 * 
 * This is done through signals and relies on `FlxInputSystem` to execute them.
 */
class FlxInputComponent extends FlxComponent
{

}