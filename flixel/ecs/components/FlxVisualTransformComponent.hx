package flixel.ecs.components;

/**
 * Stores all the data required for a `FlxSprite` to be drawn, but only the visual aspects.
 * This includes things like `origin`, `offset`, and `scale`.
 */
class FlxVisualTransformComponent extends FlxComponent
{}